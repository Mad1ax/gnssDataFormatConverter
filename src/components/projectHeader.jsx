const ProjectHeader = ({data}) => {
  return (
    <div className='marginedContainer'>
      <h1 className='m-2 border-bottom text-center'>{data}</h1>
    </div>
  );
};

export default ProjectHeader;
